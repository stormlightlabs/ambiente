import vikeSolid from 'vike-solid/config';
import type { Config } from 'vike/types';

export default {
	description: 'Compose, transform, and perform repeatable generative music with direct musical control.',
	extends: vikeSolid,
	lang: 'en',
	prerender: true,
	title: 'Ambiente'
} satisfies Config;
